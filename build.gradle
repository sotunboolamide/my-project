plugins {
  id "org.sonarqube" version "3.3"
}

sonarqube {
  properties {
    property "sonar.projectKey", "Penguin1234"
    property "sonar.organization", "testhk"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}
